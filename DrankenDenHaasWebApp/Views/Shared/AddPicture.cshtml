@model DrankenDenHaasWebApp.MultimediaViewModel

@using (Html.BeginForm("Add", "Multimedia", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()
    <div class="form-horizontal">
        <h3>First add a picture</h3>
        <hr />
        @Html.ValidationSummary(true)
        @*<div class="form-group">
                @Html.HiddenFor(model => model.MultimediaId, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.HiddenFor(model => model.MultimediaId)
                    @Html.ValidationMessageFor(model => model.MultimediaId)
                </div>
            </div>*@

        <div class="form-group">
            @Html.LabelFor(model => model.ImagePath, htmlAttributes: new { @class = "contrncol-label col-md-2" })
            <div class="col-md-10">
                @*@Html.EditorFor(model => model.ImagePath)
                    @Html.ValidationMessageFor(model => model.ImagePath)*@

                <input type="file" name="ImageFile" required />

                <br />
                @if (ViewBag.Path != null)
                {

                    <img class="img-responsive" src="@Url.Content(ViewBag.Path)" height="200" width="200" />
                    <br />
                    <strong> @ViewBag.Message </strong>
                }

            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Confirm picture" class="btn btn-default" />
            </div>
        </div>
    </div>
}

@*<div>
        @Html.ActionLink("Back to List", "Index")
    </div>*@
